<UserControl x:Class="AX2012PerformanceOptimizer.WpfApp.Views.AosMonitoringView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1200">
    
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <TextBlock Text="AOS Monitoring" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                <Button Content="🔄 Refresh" Command="{Binding RefreshCommand}" 
                       Background="#2196F3" Foreground="White" FontWeight="Bold" 
                       Width="120" Height="35" BorderThickness="0" Cursor="Hand"/>
            </StackPanel>

            <!-- AOS Metrics Cards -->
            <Border Background="#E3F2FD" BorderBrush="#2196F3" BorderThickness="2" 
                   CornerRadius="10" Padding="20" Margin="0,0,0,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Active Sessions" FontSize="14" Foreground="#1976D2"/>
                        <TextBlock Text="{Binding CurrentMetrics.ActiveUserSessions}" 
                                  FontSize="32" FontWeight="Bold" Foreground="#0D47A1" Margin="0,5,0,0"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Server Name" FontSize="14" Foreground="#1976D2"/>
                        <TextBlock Text="{Binding CurrentMetrics.ServerName}" 
                                  FontSize="18" FontWeight="Bold" Foreground="#0D47A1" Margin="0,5,0,0"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Status" FontSize="14" Foreground="#1976D2"/>
                        <TextBlock Text="{Binding CurrentMetrics.IsHealthy}" 
                                  FontSize="18" FontWeight="Bold" Foreground="#0D47A1" Margin="0,5,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- User Sessions -->
            <TextBlock Text="Active User Sessions" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
            <ListBox ItemsSource="{Binding UserSessions}" MaxHeight="400">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock Grid.Column="0" Text="{Binding UserId}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding ClientComputer}"/>
                            <TextBlock Grid.Column="2" Text="{Binding LoginDateTime, StringFormat='yyyy-MM-dd HH:mm:ss'}"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </ScrollViewer>
</UserControl>


