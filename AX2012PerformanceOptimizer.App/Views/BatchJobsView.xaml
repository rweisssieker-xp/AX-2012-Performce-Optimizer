<Page
    x:Class="AX2012PerformanceOptimizer.App.Views.BatchJobsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Batch Jobs" Style="{StaticResource TitleTextBlockStyle}"/>
            <TextBlock Text="Monitor batch job execution and performance" 
                      Style="{StaticResource BodyTextBlockStyle}" 
                      Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                      Margin="0,8,0,0"/>
        </StackPanel>

        <!-- Content -->
        <ScrollViewer Grid.Row="1">
            <StackPanel Spacing="16">
                <!-- Toolbar -->
                <CommandBar DefaultLabelPosition="Right">
                    <AppBarButton Icon="Refresh" Label="Refresh" Command="{x:Bind ViewModel.RefreshCommand}"/>
                </CommandBar>

                <!-- Running Jobs -->
                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                       CornerRadius="8" Padding="20">
                    <StackPanel>
                        <TextBlock Text="Running Batch Jobs" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,0,0,12"/>
                        <ListView ItemsSource="{x:Bind ViewModel.RunningJobs, Mode=OneWay}" 
                                 MaxHeight="300">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Grid Padding="8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="2*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <TextBlock Grid.Column="0" Text="{Binding JobDescription}" FontWeight="SemiBold"/>
                                        <TextBlock Grid.Column="1" Text="{Binding Status}"/>
                                        <TextBlock Grid.Column="2" Text="{Binding Company}"/>
                                        <TextBlock Grid.Column="3" Text="{Binding StartDateTime}"/>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>
                </Border>

                <!-- Failed Jobs -->
                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                       CornerRadius="8" Padding="20">
                    <StackPanel>
                        <TextBlock Text="Failed Batch Jobs" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,0,0,12"/>
                        <ListView ItemsSource="{x:Bind ViewModel.FailedJobs, Mode=OneWay}" 
                                 MaxHeight="300">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Grid Padding="8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="2*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <TextBlock Grid.Column="0" Text="{Binding JobDescription}" FontWeight="SemiBold" Foreground="Red"/>
                                        <TextBlock Grid.Column="1" Text="{Binding Status}"/>
                                        <TextBlock Grid.Column="2" Text="{Binding Company}"/>
                                        <TextBlock Grid.Column="3" Text="{Binding StartDateTime}"/>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>

