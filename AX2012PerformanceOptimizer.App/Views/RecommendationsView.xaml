<Page
    x:Class="AX2012PerformanceOptimizer.App.Views.RecommendationsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Recommendations" Style="{StaticResource TitleTextBlockStyle}"/>
            <TextBlock Text="AI-generated optimization recommendations based on current performance data" 
                      Style="{StaticResource BodyTextBlockStyle}" 
                      Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                      Margin="0,8,0,0"/>
        </StackPanel>

        <!-- Content -->
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Toolbar -->
            <CommandBar Grid.Row="0" DefaultLabelPosition="Right">
                <AppBarButton Icon="Refresh" Label="Refresh" Command="{x:Bind ViewModel.RefreshCommand}"/>
                <AppBarButton Icon="Copy" Label="Copy Script" Command="{x:Bind ViewModel.CopyScriptCommand}"/>
                <AppBarButton Icon="Accept" Label="Mark Implemented" Command="{x:Bind ViewModel.MarkAsImplementedCommand}"/>
            </CommandBar>

            <!-- Recommendations List -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="3*"/>
                </Grid.ColumnDefinitions>

                <!-- Recommendations ListView -->
                <ListView Grid.Column="0" 
                         ItemsSource="{x:Bind ViewModel.Recommendations, Mode=OneWay}"
                         SelectedItem="{x:Bind ViewModel.SelectedRecommendation, Mode=TwoWay}"
                         Margin="0,0,8,0">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="12" Margin="0,4">
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <Border Background="{ThemeResource AccentFillColorDefaultBrush}" 
                                               CornerRadius="4" Padding="6,2">
                                            <TextBlock Text="{Binding Priority}" 
                                                      FontSize="12" 
                                                      Foreground="White"/>
                                        </Border>
                                        <Border Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" 
                                               CornerRadius="4" Padding="6,2">
                                            <TextBlock Text="{Binding Category}" 
                                                      FontSize="12"/>
                                        </Border>
                                    </StackPanel>
                                    <TextBlock Text="{Binding Title}" 
                                              FontWeight="SemiBold" 
                                              Margin="0,8,0,0"/>
                                    <TextBlock Text="{Binding Description}" 
                                              TextWrapping="Wrap"
                                              Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                              Margin="0,4,0,0"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <!-- Details Panel -->
                <Border Grid.Column="1" 
                       Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                       CornerRadius="8" Padding="20">
                    <ScrollViewer>
                        <StackPanel Spacing="16">
                            <TextBlock Text="{x:Bind ViewModel.SelectedRecommendation.Title, Mode=OneWay}" 
                                      Style="{StaticResource SubtitleTextBlockStyle}"/>

                            <StackPanel Spacing="4">
                                <TextBlock Text="Priority:" FontWeight="SemiBold"/>
                                <TextBlock Text="{x:Bind ViewModel.SelectedRecommendation.Priority, Mode=OneWay}"/>
                            </StackPanel>

                            <StackPanel Spacing="4">
                                <TextBlock Text="Category:" FontWeight="SemiBold"/>
                                <TextBlock Text="{x:Bind ViewModel.SelectedRecommendation.Category, Mode=OneWay}"/>
                            </StackPanel>

                            <StackPanel Spacing="4">
                                <TextBlock Text="Description:" FontWeight="SemiBold"/>
                                <TextBlock Text="{x:Bind ViewModel.SelectedRecommendation.Description, Mode=OneWay}" 
                                          TextWrapping="Wrap"/>
                            </StackPanel>

                            <StackPanel Spacing="4">
                                <TextBlock Text="Impact Analysis:" FontWeight="SemiBold"/>
                                <TextBlock Text="{x:Bind ViewModel.SelectedRecommendation.ImpactAnalysis, Mode=OneWay}" 
                                          TextWrapping="Wrap"/>
                            </StackPanel>

                            <StackPanel Spacing="4">
                                <TextBlock Text="Action Script:" FontWeight="SemiBold"/>
                                <TextBox Text="{x:Bind ViewModel.SelectedRecommendation.ActionScript, Mode=OneWay}" 
                                        IsReadOnly="True"
                                        TextWrapping="Wrap"
                                        Height="200"
                                        FontFamily="Consolas"
                                        AcceptsReturn="True"/>
                            </StackPanel>

                            <StackPanel Spacing="8" Orientation="Horizontal">
                                <Button Content="Copy Script" Command="{x:Bind ViewModel.CopyScriptCommand}">
                                    <Button.Icon>
                                        <FontIcon Glyph="&#xE8C8;"/>
                                    </Button.Icon>
                                </Button>
                                <Button Content="Mark as Implemented" 
                                       Command="{x:Bind ViewModel.MarkAsImplementedCommand}"
                                       Style="{StaticResource AccentButtonStyle}">
                                    <Button.Icon>
                                        <FontIcon Glyph="&#xE73E;"/>
                                    </Button.Icon>
                                </Button>
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</Page>

